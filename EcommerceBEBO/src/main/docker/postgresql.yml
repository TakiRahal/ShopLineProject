version: '2'
services:
  ecommercebackend-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/EcommerceBackend/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=EcommerceBackend
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
